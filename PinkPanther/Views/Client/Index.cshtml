@model ClientViewModel
@{
    ViewData["Title"] = "Client Page";
}

single client's data and list of adopted animals

@if(Model.Animals != null)
{
    @foreach (var animal in Model.Animals)
    {
        <p>@animal.Name</p>
    }
}
